import streamlit as st
import pandas as pd
import numpy as np

st.set_page_config(page_title="KOSIS Macro→Investment", layout="wide")

st.title("KOSIS Macro→Investment Inference (MVP)")
st.write("Upload or fetch series, view correlation atlas, run VAR IRF, and simulate scenarios.")

st.header("Coming Soon") 
st.info("This is a skeleton. Use main.py for demo runs. Plug in KOSIS API and build views.")
